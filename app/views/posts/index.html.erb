<h1>Timeline </h1>

<%= form_for   @post, url: {action: "create"} do |f| %>
  <%= f.label :body, 'Your post here' %>
  <%= f.text_field :body %>
  <%= f.submit "Submit" %>
<% end %>


<% @posts.each do |post| %>
  <p><%= post.user.name %></p>
  <p><%= post.body %></p>
  <hr>
<% end %>
